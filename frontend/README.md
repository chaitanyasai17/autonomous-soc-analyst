# ASOC Frontend

React + TypeScript + Vite application for the ASOC dashboard.

> Placeholder only — dependencies and app code are added in Part 13 — React
> Frontend & Dashboard. `package.json` and `tsconfig.json` intentionally
> contain no dependencies yet.

## Planned Stack
React, TypeScript, Vite, Tailwind CSS, React Router, Axios, TanStack Query, Recharts.

## Folder Guide
See per-folder `README.md` files under `src/` for the responsibility of each
directory (notably `api/` vs `services/`, and `components/ui/` vs
`components/common/`, which are the two most easily confused pairs).
