# components/common/

Reusable, application-aware components shared across multiple features
(e.g., PageHeader, EmptyState, ConfirmDialog). May know about app-level
concepts like alerts/incidents, but are not tied to one specific page.
